enum UserRole {
    USER
    HOST
    ADMIN
}

enum UserStatus {
    ACTIVE
    INACTIVE
    SUSPENDED
    DELETED
    BLOCKED
}

enum VerificationStatus {
    PENDING
    VERIFIED
    REJECTED
}

enum HostContactPreference {
    EMAIL
    PHONE
    BOTH
}

enum Gender {
    MALE
    FEMALE
}
